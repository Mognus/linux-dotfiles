# Local Services

## Redis

```bash
sudo systemctl enable --now redis   # start + autostart
sudo systemctl start redis          # start only
sudo systemctl stop redis           # stop
```

## Docker

```bash
sudo systemctl enable --now docker  # start + autostart
sudo systemctl start docker         # start only
```
