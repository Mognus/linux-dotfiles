# =============================================================================
# Arch Linux Packages
# Install with: ./install.sh
# =============================================================================

# --- Shell ---
zsh                         # Shell
fish                        # Friendly interactive shell
tmux                        # Terminal multiplexer
zsh-completions             # Extra Zsh completions
fzf                         # Fuzzy finder (Ctrl+R history, Ctrl+T files, Alt-C dirs)
fd                          # Fast find, used as fzf backend
zoxide                      # Smart cd replacement (z, zi)
zsh-autosuggestions         # Command suggestions from history
zsh-syntax-highlighting     # Syntax highlighting in the shell

# --- Editor ---
neovim                      # Editor

# --- Terminal ---
alacritty                   # Terminal emulator

# --- Wayland / Desktop ---
hyprland                    # Wayland compositor / WM
waybar                      # Status bar
rofi-wayland                # App launcher
xdg-desktop-portal-hyprland # Screen sharing, file picker for Wayland apps
xdg-user-dirs               # Creates ~/Downloads, ~/Pictures etc.
dunst                       # Notification daemon
hyprlock                    # Lock screen
hyprpaper                   # Wallpaper daemon
hypridle                    # Idle daemon (lock/turn off screen)
polkit-gnome                # Privilege dialog (sudo popups in GUI)

# --- Wayland Tools ---
grim                        # Screenshot (full screen)
slurp                       # Select screen region for screenshots
wl-clipboard                # Clipboard (wl-copy, wl-paste)
swappy                      # Screenshot editor
socat                       # Socket communication (e.g. Hyprland IPC)
jq                          # JSON processing in CLI

# --- Audio ---
pipewire                    # Audio server
wireplumber                 # Pipewire session manager
pipewire-alsa               # ALSA compatibility via Pipewire
pipewire-pulse              # PulseAudio compatibility via Pipewire
alsa-utils                  # ALSA CLI tools (alsamixer etc.)
pavucontrol                 # Audio mixer GUI

# --- Bluetooth ---
bluez                       # Bluetooth stack
bluez-utils                 # Bluetooth CLI tools (bluetoothctl)

# --- Fonts ---
ttf-meslo-nerd              # Nerd Font (Meslo)
ttf-jetbrains-mono-nerd     # Nerd Font (JetBrains Mono)

# --- Apps ---
firefox                     # Browser
discord                     # Chat
obsidian                    # Notes (Markdown vault)

# --- Dev Languages ---
rustup                      # Rust toolchain manager
go                          # Go
nodejs                      # Node.js
npm                         # Node package manager

# --- Containers ---
docker                      # Container runtime
docker-compose              # Multi-container orchestration

# --- File Manager ---
nnn                         # Terminal file manager
thunar                      # GUI file manager

# --- Media ---
yt-dlp                      # YouTube/video downloader
playerctl                   # Media key / playback control (Waybar integration)
mpv                         # Video player

# --- Tools ---
git                         # Version control
curl                        # HTTP requests
wget                        # File download
openssh                     # SSH client/server
man-db                      # Man pages
ripgrep                     # Fast grep (rg)
bat                         # Better cat (syntax highlighting)
btop                        # Resource monitor
stow                        # Dotfiles symlink manager
github-cli                  # GitHub CLI (gh)
fastfetch                   # System info (neofetch replacement)
networkmanager              # Network management
unzip                       # ZIP extraction
7zip                        # 7z archives
reflector                   # Update pacman mirrorlist
ufw                         # Firewall (deny incoming, allow outgoing)

# =============================================================================
# [AUR] — installed via yay
# =============================================================================

mullvad-vpn                 # VPN
tor-browser                 # Tor Browser (.onion access)
